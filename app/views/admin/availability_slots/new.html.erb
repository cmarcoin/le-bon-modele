<% content_for :title, "Nouveau creneau | Admin" %>

<%= render "admin/nav" %>

<main class="px-6 py-10">
  <div class="mx-auto max-w-3xl rounded-[28px] border border-[#E7E2DC] bg-white p-8 shadow-soft">
    <h1 class="font-heading text-4xl font-bold text-brand-primary">Ajouter un creneau</h1>
    <%= render "form", availability_slot: @availability_slot %>
  </div>
</main>
